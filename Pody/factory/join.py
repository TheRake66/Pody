


class Join:
    """Enumération des types de jointure.
    """
    
    INNER = 'INNER'  # type: str # INNER JOIN
    LEFT = 'LEFT'    # type: str # LEFT JOIN
    RIGHT = 'RIGHT'  # type: str # RIGHT JOIN
    FULL = 'FULL'    # type: str # FULL JOIN